# If running frontend separately from backend (different ports):
# VITE_API_URL=http://localhost:5000/api

# If using the Vite proxy (default, recommended for dev):
# Leave VITE_API_URL unset — the proxy handles it automatically
